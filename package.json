{
  "name": "shikav-sql",
  "version": "1.0.0",
  "description": "Shikav [by Kavish Ghumre] - Interactive SQL, Java & .NET Learning Platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "seed": "node seed.js",
    "install:client": "cd client && npm install",
    "build:client": "cd client && npm run build",
    "build": "npm run install:client && npm run build:client",
    "dev": "concurrently \"npm run start\" \"cd client && npm run dev\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "sql.js": "^1.11.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
